import weather from 'src/api/weather-api';
import storage from 'src/api/storage-api';
import geolocation from 'src/api/geolocation-api';

export default {
    weather,
    storage,
    geolocation
};
